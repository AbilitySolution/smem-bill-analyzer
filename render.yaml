services:
  - type: web
    name: smem-bill-analyzer-staging
    env: node
    plan: free
    buildCommand: npm install && npm run build && pip3 install -r scripts/reports/requirements.txt
    startCommand: npx next start -p $PORT
    healthCheckPath: /login
    envVars:
      - key: NODE_ENV
        value: production
